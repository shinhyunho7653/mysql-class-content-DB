# 정보처리기사 문제 정리

작성일: 2026-07-29

## 1. 데이터베이스 기본 개념

### 문제 1
다음 중 관계형 데이터베이스에서 테이블의 열을 의미하는 것은?
1. 레코드
2. 필드
3. 튜플
4. 엔티티

정답: 2. 필드
해설: 테이블의 열은 일반적으로 필드라고 하며, 행은 레코드 또는 튜플로 표현됩니다.

### 문제 2
다음 중 기본 키(Primary Key)의 특징이 아닌 것은?
1. 중복 값을 가질 수 없다.
2. NULL 값을 가질 수 없다.
3. 여러 개의 컬럼을 조합할 수 있다.
4. 항상 자동으로 생성된다.

정답: 4. 항상 자동으로 생성된다.
해설: 기본 키는 설계자가 지정하며, 자동 생성되는 것은 자동 증가 컬럼과 같은 경우만 해당됩니다.

### 문제 3
외래 키(Foreign Key)의 역할은 무엇인가?
1. 데이터의 무결성을 보장한다.
2. 테이블의 이름을 바꾼다.
3. 인덱스를 생성한다.
4. 저장 공간을 절약한다.

정답: 1. 데이터의 무결성을 보장한다.
해설: 외래 키는 다른 테이블의 기본 키와 관계를 맺어 참조 무결성을 유지합니다.

## 2. SQL 기초

### 문제 4
다음 SQL 중 데이터를 조회하는 명령어는?
1. INSERT
2. UPDATE
3. SELECT
4. DELETE

정답: 3. SELECT
해설: SELECT는 테이블에서 데이터를 조회할 때 사용합니다.

### 문제 5
다음 중 WHERE 절의 역할은 무엇인가?
1. 결과를 정렬한다.
2. 특정 조건을 만족하는 행을 필터링한다.
3. 중복을 제거한다.
4. 테이블을 생성한다.

정답: 2. 특정 조건을 만족하는 행을 필터링한다.
해설: WHERE 절은 조건에 맞는 행만 추출하는 데 사용합니다.

### 문제 6
다음 중 ORDER BY 절의 역할은 무엇인가?
1. 행을 추가한다.
2. 결과를 정렬한다.
3. 중복을 제거한다.
4. 테이블을 삭제한다.

정답: 2. 결과를 정렬한다.
해설: ORDER BY는 조회 결과를 오름차순 또는 내림차순으로 정렬합니다.

## 3. 집계 함수와 그룹화

### 문제 7
다음 중 집계 함수가 아닌 것은?
1. COUNT()
2. SUM()
3. AVG()
4. LIKE()

정답: 4. LIKE()
해설: LIKE는 패턴 비교 연산자이며, 집계 함수가 아닙니다.

### 문제 8
GROUP BY 절의 핵심 목적은 무엇인가?
1. 테이블을 생성한다.
2. 같은 값을 가진 행을 묶어 집계한다.
3. 컬럼을 삭제한다.
4. 인덱스를 생성한다.

정답: 2. 같은 값을 가진 행을 묶어 집계한다.
해설: GROUP BY는 특정 컬럼 기준으로 그룹화하여 집계 결과를 얻습니다.

### 문제 9
다음 중 HAVING 절의 역할은 무엇인가?
1. 그룹화된 결과에 조건을 적용한다.
2. 테이블을 새로 만든다.
3. 데이터를 삽입한다.
4. 인덱스를 삭제한다.

정답: 1. 그룹화된 결과에 조건을 적용한다.
해설: HAVING은 GROUP BY 결과에 대해 조건을 부여할 때 사용합니다.

## 4. 정규화와 모델링

### 문제 10
정규화의 주된 목적은 무엇인가?
1. 데이터 중복을 줄이고 무결성을 높인다.
2. 속도를 빠르게 한다.
3. 저장 공간을 늘린다.
4. 테이블 수를 늘린다.

정답: 1. 데이터 중복을 줄이고 무결성을 높인다.
해설: 정규화는 이상 현상을 줄이고 데이터 구조를 안정적으로 만듭니다.

### 문제 11
다음 중 제1정규형의 특징은 무엇인가?
1. 중복된 튜플이 없도록 한다.
2. 각 컬럼의 값이 원자값이어야 한다.
3. 모든 속성이 기본 키에 완전 종속되어야 한다.
4. 다대다 관계를 분해한다.

정답: 2. 각 컬럼의 값이 원자값이어야 한다.
해설: 제1정규형은 컬럼의 값이 더 이상 분해되지 않는 원자값이어야 합니다.

### 문제 12
다음 중 ERD에서 엔티티(Entity)의 의미는 무엇인가?
1. 데이터의 흐름
2. 데이터 저장 대상
3. 데이터 처리 절차
4. 접근 권한

정답: 2. 데이터 저장 대상
해설: 엔티티는 데이터베이스에서 관리할 객체나 개념을 의미합니다.

## 5. 실무형 응용 문제

### 문제 13
다음 SQL 구문이 의미하는 것은 무엇인가?
SELECT COUNT(*) FROM customer;
1. customer 테이블의 모든 행 수를 계산한다.
2. customer 테이블의 첫 행만 조회한다.
3. customer 테이블을 삭제한다.
4. customer 테이블의 구조를 출력한다.

정답: 1. customer 테이블의 모든 행 수를 계산한다.
해설: COUNT(*)는 행의 개수를 계산합니다.

### 문제 14
다음 중 데이터베이스의 무결성에 가장 가까운 개념은 무엇인가?
1. 속도 향상
2. 저장 공간 확보
3. 데이터의 정확성과 일관성 유지
4. 백업 자동화

정답: 3. 데이터의 정확성과 일관성 유지
해설: 무결성은 데이터가 정확하고 일관되게 유지되는 상태를 의미합니다.

### 문제 15
다음 중 JOIN의 목적은 무엇인가?
1. 테이블을 나눈다.
2. 서로 관련된 테이블의 데이터를 결합한다.
3. 데이터를 삭제한다.
4. 인덱스를 생성한다.

정답: 2. 서로 관련된 테이블의 데이터를 결합한다.
해설: JOIN은 두 개 이상의 테이블을 연결해 관련 데이터를 조회합니다.

## 6. 요약
- 기본 키는 테이블의 식별자 역할을 한다.
- 외래 키는 참조 무결성을 유지한다.
- SELECT는 데이터를 조회한다.
- GROUP BY와 HAVING은 집계 결과를 다룰 때 사용한다.
- 정규화는 데이터 중복을 줄이고 구조를 안정적으로 만든다.
